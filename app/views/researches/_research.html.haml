%article
  - if research.image?
    = image_tag research.image.url(:thumb), :class => :aside
  %div
    %header
      %h1.subtitle= research.title
    %p= raw research.description
    %h3 Projetos:
    %ul
      - for project in research.projects
        %li= link_to project.title, project
